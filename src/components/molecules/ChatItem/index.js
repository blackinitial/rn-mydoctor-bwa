import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ChatItem = () => {
  return (
    <View>
      <Text>ChatItem comp</Text>
    </View>
  );
};

export default ChatItem;

const styles = StyleSheet.create({});
